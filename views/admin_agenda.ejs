<!doctype html>
<html lang="es" data-theme="light">
<head>
    <%- include('partials/head', {
    title: 'Agenda',
    description: 'Agenda administrativa de salidas y reservas.',
    noindex: true
    }) %>

    <link href="/css/admin.css" rel="stylesheet">

    <style>
        .hero {
            border: none;
            background:
                radial-gradient(720px 280px at 100% -20%, color-mix(in srgb, var(--wine) 12%, transparent), transparent 55%),
                radial-gradient(860px 240px at -8% -40%, color-mix(in srgb, var(--wine) 18%, transparent), transparent 70%),
                color-mix(in srgb, var(--card) 92%, transparent);
            box-shadow: var(--shadow);
            border-radius: 20px;
            padding: 1.15rem 1.2rem 1.2rem;
        }

        .hero-intro {
            min-width: min(100%, 280px);
        }

        .hero-intro .subtle {
            max-width: 34rem;
            line-height: 1.45;
        }

        @media (prefers-reduced-motion: reduce) {
            .agenda-kpi:hover,
            .trip-card:hover {
                transform: none;
            }
        }

        .hero-toolbar-wrap {
            flex: 1 1 260px;
            min-width: min(100%, 280px);
            justify-content: flex-end;
        }

        .toolbar-form {
            display: flex;
            flex-wrap: wrap;
            gap: .55rem;
            align-items: center;
            justify-content: flex-end;
        }

        /* KPIs del día (hero) */
        .agenda-kpis {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(158px, 1fr));
            gap: .65rem;
            margin-top: 1rem;
            max-width: 26rem;
        }

        .agenda-kpi {
            position: relative;
            border-radius: 16px;
            padding: .78rem .95rem .82rem 1.05rem;
            border: none;
            background: linear-gradient(
                145deg,
                color-mix(in srgb, var(--card) 94%, transparent),
                color-mix(in srgb, var(--wine) 7%, var(--card))
            );
            box-shadow:
                0 1px 0 rgba(255, 255, 255, .06) inset,
                0 8px 22px rgba(0, 0, 0, .06);
            overflow: hidden;
            transition: box-shadow .2s ease, transform .18s ease;
        }

        .agenda-kpi:hover {
            box-shadow:
                0 1px 0 rgba(255, 255, 255, .06) inset,
                0 10px 26px rgba(0, 0, 0, .09);
            transform: translateY(-1px);
        }

        .agenda-kpi::before {
            content: "";
            position: absolute;
            left: 0;
            top: 0;
            bottom: 0;
            width: 4px;
            border-radius: 16px 0 0 16px;
            background: linear-gradient(180deg, var(--wine), var(--wine-2));
        }

        .agenda-kpi--packages::before {
            background: linear-gradient(
                180deg,
                color-mix(in srgb, var(--wine-2) 90%, var(--muted)),
                color-mix(in srgb, var(--wine) 55%, var(--muted))
            );
        }

        .agenda-kpi .label {
            color: var(--muted);
            font-size: .78rem;
            font-weight: 600;
            letter-spacing: .015em;
            line-height: 1.25;
        }

        .agenda-kpi .value {
            font-size: 1.65rem;
            font-weight: 750;
            font-variant-numeric: tabular-nums;
            letter-spacing: -.035em;
            line-height: 1.1;
            margin-top: .25rem;
            color: var(--text);
        }

        .stat {
            border: 1px solid var(--border);
            background: color-mix(in srgb, var(--card) 88%, transparent);
            border-radius: 14px;
            padding: .55rem .75rem;
            min-width: 148px;
            box-shadow: 0 1px 0 rgba(255, 255, 255, .04) inset;
        }

        .stat .label {
            color: var(--muted);
            font-size: .78rem;
            font-weight: 600;
        }

        .stat .value {
            font-weight: 650;
            font-variant-numeric: tabular-nums;
        }

        .trip-card {
            position: relative;
            background: var(--card);
            border: 1px solid var(--border);
            box-shadow: var(--shadow);
            border-radius: 18px;
            overflow: hidden;
            transition: transform .18s ease, box-shadow .18s ease, border-color .18s ease;
        }

        .trip-card:hover {
            transform: translateY(-2px);
            border-color: color-mix(in srgb, var(--wine) 42%, var(--border));
            box-shadow: 0 12px 30px rgba(0, 0, 0, .12);
        }

        .trip-card .top {
            padding: 16px 16px 12px 18px;
        }

        .trip-card .bottom {
            padding: 12px 16px 14px 18px;
            border-top: 1px solid var(--border);
            background: color-mix(in srgb, var(--card) 90%, transparent);
        }

        .route-title {
            margin: 0 0 2px;
            font-size: 1.05rem;
            font-weight: 650;
            letter-spacing: -.01em;
        }

        .route-time {
            color: var(--wine);
            font-weight: 700;
        }

        .chip-row {
            display: flex;
            flex-wrap: wrap;
            gap: .45rem;
            margin-top: .7rem;
        }

        .chip-soft {
            display: inline-flex;
            align-items: center;
            border: 1px solid var(--border);
            border-radius: 999px;
            background: color-mix(in srgb, var(--card) 78%, transparent);
            color: var(--text);
            font-size: .83rem;
            padding: .32rem .62rem;
            font-weight: 550;
            transition: border-color .15s ease, background .15s ease;
        }

        .trip-card:hover .chip-soft {
            border-color: color-mix(in srgb, var(--wine) 22%, var(--border));
            background: color-mix(in srgb, var(--wine) 6%, var(--card));
        }

        .progress-soft {
            height: 10px;
            border-radius: 999px;
            background: color-mix(in srgb, var(--muted) 18%, transparent);
            overflow: hidden;
            border: 1px solid var(--border);
        }

        .progress-soft > span {
            display: block;
            height: 100%;
            width: var(--w, 0%);
            background: linear-gradient(90deg, var(--wine), var(--wine-2));
            border-radius: 999px;
        }

        .muted {
            color: var(--muted);
        }

        .empty-state {
            display: none;
        }

        .empty-state.show {
            display: block;
        }
    </style>
</head>


<body class="admin-app">
<nav class="navbar navbar-dark app-navbar">
    <div class="container d-flex justify-content-between align-items-center">
        <a class="navbar-brand d-flex align-items-center gap-2" href="/admin/agenda">
            <span class="brand-dot"></span>
            <span class="fw-semibold">Admin - Agenda</span>
        </a>

        <div class="d-flex align-items-center gap-2">
            <button id="themeToggle" class="theme-btn" type="button" aria-label="Cambiar tema">
                <span id="themeIcon">&#x1F319;</span> <span class="d-none d-sm-inline">Tema</span>
            </button>

            <form method="POST" action="/admin/logout" class="m-0">
                <button class="btn btn-sm btn-outline-light rounded-4">Salir</button>
            </form>
        </div>
    </div>
</nav>

<main class="container py-4">

    <div class="hero mb-3">
        <div class="d-flex flex-wrap justify-content-between align-items-start gap-3">
            <div class="hero-intro flex-grow-1">
                <div class="pill">Agenda</div>
                <h1 class="h3 page-title mt-2 mb-1">Salidas del dia</h1>
                <div class="subtle">Cupo, servicios de paqueteria y accesos por salida.</div>
                <div class="agenda-kpis" role="group" aria-label="Resumen del dia">
                    <div class="agenda-kpi agenda-kpi--passengers">
                        <div class="label">Pasajeros totales (este dia)</div>
                        <div class="value"><%= Number(totalPassengers || 0) %></div>
                    </div>
                    <div class="agenda-kpi agenda-kpi--packages">
                        <div class="label">Paquetes del dia</div>
                        <div class="value"><%= Number(totalPackages || 0) %></div>
                    </div>
                </div>
            </div>

            <div class="d-flex flex-wrap gap-2 align-items-center hero-toolbar-wrap">

                <form id="agendaForm" class="toolbar-form" method="GET" action="/admin/agenda">
                    <input id="agendaDate" type="date" name="date" class="form-control" value="<%= date %>">
                    <input type="hidden" name="onlyWithTrip" id="onlyWithTripInput" value="0">
                    <button class="btn btn-wine rounded-4 px-4 fw-semibold">Ver</button>

                    <a id="onlyToggleBtn" class="btn btn-ghost px-3 fw-semibold"
                       href="/admin/agenda?date=<%= encodeURIComponent(date || '') %>&onlyWithTrip=1">
                        Solo con viaje
                    </a>
                    <!-- ✅ “Tab” a otra página -->
                    <a class="btn btn-ghost px-3 fw-semibold"
                       href="/admin/recent">
                        Ultimos registros
                    </a>

                    <a class="btn btn-ghost px-3 fw-semibold"
                       href="/admin/horarios">
                        Horarios
                    </a>
                </form>
            </div>
        </div>
    </div>

    <div id="emptyState" class="card-soft p-4 empty-state">
        <div class="h6 mb-1">Sin salidas con viaje</div>
        <div class="subtle">No hay pasajeros ni servicios de paqueteria en este dia con el filtro activo.</div>
    </div>

    <div class="row g-3" id="tripsGrid">
        <% if (!trips || trips.length === 0) { %>
            <div class="col-12">
                <div class="card-soft p-4">
                    <div class="h6 mb-1">Sin salidas</div>
                    <div class="subtle">Cambia la fecha para ver otras salidas.</div>
                </div>
            </div>
        <% } %>

        <% (trips || []).forEach(t => {
            const used = Number(t.used_seats || 0);

            // Cap maximo 6 en agenda (aunque DB traiga otro valor)
            const capRaw = Number(t.capacity_passengers || 0);
            const cap = Math.min(6, Math.max(0, capRaw));

            const pct  = cap > 0 ? Math.min(100, Math.round((used / cap) * 100)) : 0;
            const remaining = Math.max(0, cap - used);

            const packs = Number(t.packages || 0);
            const hhmm = String(t.depart_time || '').slice(0,5);
        %>

        <div class="col-lg-6 trip-item"
             data-used="<%= used %>"
             data-packages="<%= packs %>">
            <div class="trip-card">
                <div class="top">
                    <div class="d-flex justify-content-between align-items-start gap-3">
                        <div>
                            <div class="route-title">
                                <%= directionLabel(t.direction) %> <span class="muted">→</span> <span
                                        class="route-time"><%= hhmm %></span>
                            </div>
                            <div class="subtle small">Fecha: <%= t.trip_date %></div>
                        </div>

                        <div class="text-end">
                            <div class="stat">
                                <div class="label">Cupo</div>
                                <div class="value">
                                    <%= used %>/<%= cap %>
                                    <span class="muted">- <%= remaining %> libres</span>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="mt-3">
                        <div class="d-flex justify-content-between small subtle mb-1">
                            <span>Ocupacion</span>
                            <span><%= pct %>%</span>
                        </div>
                        <div class="progress-soft" style="--w:<%= pct %>%">
                            <span></span>
                        </div>
                    </div>

                    <div class="chip-row">
                        <span class="chip-soft">Pasajeros: <%= used %></span>
                        <span class="chip-soft">Paqueteria: <%= packs %></span>
                    </div>
                </div>

                <div class="bottom d-flex justify-content-between align-items-center">
                    <div class="subtle small">Pagos, tickets y cancelaciones.</div>

                    <a class="btn btn-wine btn-sm rounded-4 px-3 fw-semibold"
                       href="/admin/trip/<%= t.trip_id %>">
                        Ver detalle
                    </a>
                </div>
            </div>
        </div>

        <% }) %>
    </div>

</main>

<script src="/js/theme.js"></script>

<script>
    (function(){
        const params = new URLSearchParams(window.location.search);
        const only = params.get('onlyWithTrip') === '1';

        const onlyInput = document.getElementById('onlyWithTripInput');
        if (onlyInput) onlyInput.value = only ? '1' : '0';

        const form = document.getElementById('agendaForm');
        const dateInput = document.getElementById('agendaDate');
        dateInput?.addEventListener('change', () => {
            form?.requestSubmit();
        });

        const toggle = document.getElementById('onlyToggleBtn');
        const dateVal = (params.get('date') || "<%= String(date || '') %>");
        const dateEnc = encodeURIComponent(dateVal || '');

        if (toggle){
            if (only){
                toggle.classList.remove('btn-ghost');
                toggle.classList.add('btn-wine');
                toggle.textContent = 'Ver todas';
                toggle.href = `/admin/agenda?date=${dateEnc}&onlyWithTrip=0`;
            } else {
                toggle.classList.remove('btn-wine');
                toggle.classList.add('btn-ghost');
                toggle.textContent = 'Solo con viaje';
                toggle.href = `/admin/agenda?date=${dateEnc}&onlyWithTrip=1`;
            }
        }

        const items = Array.from(document.querySelectorAll('.trip-item'));
        let visible = 0;

        items.forEach(el => {
            const used = Number(el.getAttribute('data-used') || 0);
            const packs = Number(el.getAttribute('data-packages') || 0);

            const hasTrip = (used > 0) || (packs > 0);
            const show = only ? hasTrip : true;

            el.style.display = show ? '' : 'none';
            if (show) visible += 1;
        });

        const empty = document.getElementById('emptyState');
        const hadTrips = items.length > 0;

        if (empty && hadTrips && only && visible === 0){
            empty.classList.add('show');
        } else if (empty){
            empty.classList.remove('show');
        }
    })();
</script>

</body>
</html>
